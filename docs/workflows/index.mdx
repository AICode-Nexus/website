---
title: AI 工作流
description: 把 planning、contract、执行、验证、review 和 PR 收成可复制的团队级流程，而不是临场发挥。
slug: /workflows
sidebar_label: AI 工作流
tags: [ai-coding, workflows, delivery]
track: prompting-workflows
kind: hub
content_form: hub
audience: mixed
stage: starter
featured: true
domain: workflows
journey_stage: development-planning
entry_role: domain
reviewed_at: 2026-03-08
source_window_end: 2026-03-08
market_status: current
---

import {
  DecisionMatrix,
  RelatedReadings,
  ScenarioCards,
} from '@site/src/components/docs';
import {workflowCatalogByGroup, workflowGroups} from '@site/src/data/workflowCatalog.mjs';

export const groupMap = new Map(workflowCatalogByGroup.map((group) => [group.id, group]));

export const problemCards = [
  {
    id: 'maintenance',
    meta: '主线 01',
    title: '我现在就要修 bug、补测试或做小重构',
    description: '先跑维护主线，不要先读框架名。',
    href: '/docs/workflows/patterns/bugfix-refactor-test/runbook',
    bullets: ['先跑最小闭环', '再补治理和补充页'],
  },
  {
    id: 'terminal',
    meta: '主线 02',
    title: '我想让终端里的 repo pairing 成为默认做法',
    description: '先固定终端主线，再看工具和平台如何补位。',
    href: '/docs/workflows/patterns/terminal-first-repo-pairing/runbook',
    bullets: ['先跑 terminal-first runbook', '再决定 Claude Code / Codex / Gemini CLI 分工'],
  },
  {
    id: 'platform',
    meta: '主线 03',
    title: '我已经有 issue / Jira，只差把任务稳稳交进 draft PR',
    description: '先跑平台 handoff 主线，再看控制面和执行栈怎么接入。',
    href: '/docs/workflows/patterns/issue-to-draft-pr/runbook',
    bullets: ['先把 issue 写到可委派', '再看平台和本地怎么分工'],
  },
  {
    id: 'spec',
    meta: '主线 04',
    title: '我在做新功能或结构性改动，先要把边界和验收写清',
    description: '这时先跑 spec 主线，不要边做边发明范围。',
    href: '/docs/workflows/patterns/spec-first/runbook',
    bullets: ['先定目标和非目标', '再进入执行栈或控制面'],
  },
];

export const familyColumns = [
  {key: 'family', label: '工作流家族'},
  {key: 'focus', label: '先解决什么问题'},
  {key: 'read', label: '先读哪一页'},
];

export const familyRows = workflowGroups.map((group) => {
  const firstItem = groupMap.get(group.id)?.items[0];

  return {
    id: group.id,
    family: group.title,
    focus: group.description,
    read: {
      label: firstItem?.title ?? '查看该类工作流',
      href: firstItem?.docsRoot ?? '/docs/workflows',
    },
  };
});

export const workflowCards = workflowCatalogByGroup.flatMap((group) =>
  group.items.map((item) => ({
    id: item.id,
    meta: group.title,
    title: item.title,
    description: item.summary,
    href: item.docsRoot,
    bullets: group.id === 'pattern'
      ? [
          `触发信号：${item.signals[0]}`,
          `默认产物：${item.steps[0]?.artifact ?? '可 review 的合同与执行证据'}`,
          `默认入口：${item.toolFit[0]?.title ?? '相关工具或框架'}`,
        ]
      : [
          `默认进入方式：${item.entry}`,
          `更适合：${item.signals[0]}`,
          `补位方式：${item.comboPatterns[0]?.title ?? '相关工具或模式'}`,
        ],
  })),
);

export const readingOrder = [
  {
    title: '先跑一条 runbook 主线',
    href: '/docs/workflows/patterns/bugfix-refactor-test/runbook',
    description: '先让一个真实任务跑起来，再决定你缺的是模式、框架还是方法层。',
  },
  {
    title: '再回到工作流概览',
    href: '/docs/workflows',
    description: '概览页负责做分流，不负责替代 runbook 和案例。',
  },
  {
    title: '需要制度骨架时再看框架层',
    href: '/docs/workflows/frameworks/spec-kit',
    description: '当 handoff、角色和产物总是乱，再引入更重的框架。',
  },
  {
    title: '最后回到案例和 playbook',
    href: '/docs/workflows/playbooks/workflow-playbook',
    description: '把模式和框架转成日常执行方法，避免只停留在概念层。',
  },
];

# AI 工作流

这页不是“工作流总表”，而是工作流分流器。目标只有一个：把你尽快送进正确的 `runbook`，而不是让你先在框架名和术语里打转。

## 现在先做什么

- 维护类任务：去 [Bugfix / Refactor / Test Runbook](/docs/workflows/patterns/bugfix-refactor-test/runbook)。
- 终端内仓库协作：去 [Terminal-First Repo Pairing Runbook](/docs/workflows/patterns/terminal-first-repo-pairing/runbook)。
- 平台任务交接：去 [Issue / Jira -> Draft PR Runbook](/docs/workflows/patterns/issue-to-draft-pr/runbook)。
- 新功能或结构性改动：去 [Spec-First Runbook](/docs/workflows/patterns/spec-first/runbook)。
- 本地到后台 handoff：去 [Local -> Background -> Cloud Runbook](/docs/workflows/patterns/local-to-background-to-cloud/runbook)。
- 想先看真实闭环：去 [Claude Code Bugfix Loop 案例](/docs/case-studies/claude-code-bugfix-loop)。

## 先按任务选默认主线

<ScenarioCards
  title="先把当前任务跑起来"
  description="如果你当前缺的是一条可执行主线，先选 runbook；不要一上来就看框架、方法论或术语总览。"
  items={problemCards}
/>

## 如果主线已经跑通，再看家族分层

<DecisionMatrix
  title="三类工作流对象怎么分"
  description="不要一上来就看框架名。先判断你缺的是默认交付链、制度骨架，还是 daily execution 方法。"
  columns={familyColumns}
  rows={familyRows}
/>

## 从这里直接进入具体工作流簇

<ScenarioCards
  title="每个工作流簇都该回到可执行页"
  description="进入任一工作流簇后，优先读概览页和 runbook，再决定是否补治理、案例和工具组合。"
  items={workflowCards}
/>

## 推荐阅读顺序

<RelatedReadings
  title="推荐阅读顺序"
  description="第一次建设团队级 AI 工作流时，建议按这个顺序从可执行主线回到制度层。"
  items={readingOrder}
/>
