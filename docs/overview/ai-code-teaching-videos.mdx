---
title: AI Code 教学视频库
description: 聚焦近 90 天 AI coding 教学视频，按视频数和课程数双口径组织多平台公开内容，并支持目录筛选与搜索深链。
slug: /ai-code-teaching-videos
sidebar_label: 教学视频库
tags: [ai-coding, video-library, teaching-videos]
track: cross-track
kind: guide
audience: individual
stage: starter
featured: false
pillar: tools
reviewed_at: 2026-03-07
source_window_end: 2026-03-07
market_status: current
---

import TeachingVideoCatalogPage from '@site/src/components/teaching-videos/TeachingVideoCatalogPage';
import {teachingVideoCatalog} from '@site/src/data/teachingVideos';

export const lastSyncedAt = new Intl.DateTimeFormat('zh-CN', {
  dateStyle: 'medium',
  timeStyle: 'short',
  timeZone: 'Asia/Shanghai',
}).format(new Date(teachingVideoCatalog.generatedAt));

# AI Code 教学视频库

这个页面不再是手工精选列表，而是一个**近 90 天自动同步的视频中心**。目录只保留符合教学内容识别、时间窗口、字段完整性和质量阈值的公开视频，并同时给出 `videoCount` 与 `courseCount` 两个口径。

最近一次同步：**{lastSyncedAt}**

## 当前目录规则

- 只展示 `windowStart` 到 `windowEnd` 时间窗内的公开视频。
- 同一语言的重复镜像会做 canonical 去重，并保留 `alternateUrls`。
- 系列教程会拆成可播放视频条目，同时聚合课程层数据。
- 页面运行时不做任何外部抓取，所有分类、打标、排序都在同步脚本阶段完成。

## 同步入口

```bash
npm run sync:teaching-videos
npm run check:teaching-videos
npm run build
```

## 当前页面内容

下面的目录页由本地生成的数据直接渲染，支持按平台、主题、语言和时间窗口筛选。页面本身不请求外部站点，所有清洗、聚合和分类都在同步脚本阶段完成。

<TeachingVideoCatalogPage />

## 维护边界

- 这个页面优先服务“知识入口”和“视频目录索引”，不是文章内容本体。
- 质量门槛、标签规则和时间窗策略应在同步脚本与测试里调整，而不是在页面运行时临时修补。
- 如果后续扩平台，优先继续沿用“同步脚本生成本地数据”的方式，避免把实时抓取塞进构建或浏览器运行时。
